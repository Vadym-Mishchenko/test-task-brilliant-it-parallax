import { TextTableStyled } from './TableText.styled';

export const TableText = () => (
  <TextTableStyled>
    We evaluate LLMs on key benchmarks using the Eleuther AI, a framework to
    test LLMs on a large number of different evaluation tasks. The higher the
    score, the better the LLM.
  </TextTableStyled>
);
